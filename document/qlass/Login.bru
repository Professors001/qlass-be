meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "university_id": "fsci1001",
    "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
