meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "university_id": "student",
    "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
